# YUME WORLD 公式キャラクター「ルミ」デザイン設計書

## 1. デザインの方向性

YUME WORLDの画面全体を、**「夢のページを開くたびに、ルミがそっと案内してくれる場所」**として統一する。ルミは目立つマスコットではなく、利用者の創作や読書を邪魔しない小さな案内役として配置する。作品の表紙、本文、入力フォームが主役になる画面では、ルミを常設せず、空状態、初回案内、成功メッセージ、ヘルプの場面に絞って登場させる。

デザインの中心は、現在のピンクとラベンダーの柔らかい配色を維持しながら、ミルキーブルーを追加する。これにより「書く」「読む」「探す」「保存する」という機能を、同じ世界観の中で区別できる。

> **ブランドメッセージ：** ルミと一緒に、夢のページをひらこう。

## 2. ルミのビジュアル仕様

### 2.1 基本シルエット

ルミは、肩より少し長いミルキーブルーからラベンダーへのグラデーションヘアを持つ、小さな夢の妖精として描く。髪は雲のように軽く、毛先は大きく巻きすぎず、シルエットだけで柔らかさが伝わる程度にする。顔は親しみやすく、目は大きくしすぎない。読書や執筆を支える存在なので、表情は明るく落ち着いた印象を基本とする。

頭上には透明感のある星の王冠を浮かべる。王冠の中央に淡いピンクの星を置き、周囲に小さな星粒を2〜3個だけ添える。王冠を大きくすると王女キャラクターに寄りすぎるため、顔幅の約半分を超えないサイズにする。

衣装は白いワンピースをベースに、胸元の大きなハート型リボンと、淡いブルーのチュールスカートを組み合わせる。胸元のクリスタルには、必要に応じて小さく「DREAM」と表示する。英字を常に描くのではなく、ロゴ用イラストや大きな立ち絵でのみ使用する。

背中の羽は大きな蝶羽ではなく、雲を2つ重ねたような半透明の形にする。羽の内側には星を1〜3個配置する。歩いた跡の星は常時表示せず、ホバー、保存完了、公開完了などの短い演出に限定する。

### 2.2 主要カラー

| 用途 | 色 | 使用方法 |
|---|---|---|
| ルミの髪 | `#CFEFFF` → `#DCC9F5` | ミルキーブルーからラベンダーへの縦または斜めグラデーション |
| ルミのリボン | `#A9DDF0` | 髪の左右と小さなボタン |
| ルミのハート | `#F3B6D4` | 胸元、宝石、選択状態 |
| ルミの衣装 | `#FFFDFE` | 白。背景と同化しないよう薄いラベンダーの影を入れる |
| 王冠 | `#EBCB78` | ゴールド。彩度を抑え、上品に見せる |
| ブランドピンク | `#D99BC0` | 主ボタン、見出し、強調 |
| ブランドラベンダー | `#BCA9E8` | グラデーションの終点、補助ボタン |
| ブランドブルー | `#A9DDF0` | 読む、検索、情報表示の補助色 |
| 本文色 | `#625665` | 長文の可読性を優先した濃い紫グレー |
| 薄い背景 | `#FFF7FB` / `#F2F1FF` | ページ背景のグラデーション |

### 2.3 表情セット

最初に6種類の表情・ポーズを用意すると、各ページで使い回しやすい。

1. **基本立ち絵**：片手にドリームペン。ホームのロゴ横に使用する。
2. **案内ポーズ**：片手をページ内へ向ける。各ページの見出しや初回案内に使用する。
3. **執筆ポーズ**：空中に文字を書いている。小説を書くページに使用する。
4. **読書ポーズ**：本の上に座り、ページをめくっている。本棚と作品ページに使用する。
5. **発見ポーズ**：虫眼鏡を持ち、星を見つけている。検索ページに使用する。
6. **応援ポーズ**：ハートを抱えている。お気に入り、いいね、コメント成功時に使用する。

表情差分は、通常、笑顔、驚き、困り顔、祝福の5種類を作る。困り顔はエラーを責める場面ではなく、「まだ作品がありません」「公開前です」といった空状態に使う。

## 3. 共通UIシステム

### 3.1 ヘッダー

ヘッダー左側に小さな「YUME WORLD」ロゴを置き、ロゴの近くにルミのミニ立ち絵を配置する。デスクトップではルミを高さ40〜52pxで表示し、スマートフォンではロゴ横の星アイコンに縮小してもよい。ヘッダーの右側にはログイン状態とアカウント選択を置く。

ナビゲーションはページ名を優先し、絵文字だけに依存しない。アイコンは補助として使用する。現在のページは、ピンクの下線と小さな星で示す。ホバー時にルミの周囲にある夢のかけらが1個だけ浮く程度の演出にする。

### 3.2 ボタン

主ボタンは、ピンクからラベンダーへのグラデーションと白文字を使う。お気に入りやいいねは、押す前を白背景のアウトライン、押した後を淡いピンクの塗りにする。ボタンの角丸は18〜24pxとし、画面全体を幼くしすぎない。

ボタンのラベルは状態を明示する。「お気に入り」から「お気に入り済み」へ、「いいね」から「いいね済み」へ変える。状態変化の瞬間だけ、ハートが1回だけ小さく拡大する。連打してもアニメーションが蓄積しないようにする。

### 3.3 夢のかけら

ハート、星、きらめき、雲、リボンを、装飾として少量使用する。装飾はカードの角、見出しの横、成功メッセージの周辺に限定する。本文の行間、入力欄の内側、長いリストの各行には置かない。

### 3.4 ルミ案内カード

ルミを表示する案内カードは、以下の構造に統一する。

- 左側または上側にルミのイラスト。
- 中央に短い一言。
- 必要な場合だけ右側に1つの行動ボタン。
- 背景は白に近い薄いブルーまたはピンク。
- カード内の文章は最大2〜3行。

例として、作品がない場合は「ここから、最初の物語が生まれるよ♡」と表示し、「小説を書く」ボタンを置く。ルミは説明文を長く読ませる役割にはしない。

## 4. ページ別デザイン

### 4.1 ホーム `index.html`

ホームはYUME WORLDの顔なので、ルミを最も大きく見せる。ただし、ヒーローエリアの中央を作品紹介に使えるよう、ルミはロゴの右下またはヒーローの片側に配置する。

上部には「YUME WORLD」と「あなたの夢を、物語に。」を置く。ロゴの「O」の中に小さな星を入れ、ルミがその横に座っている構図にする。ヒーローでは、ルミがドリームペンで空中に本の輪郭を描いている。描いた本の中に「読む」「書く」「探す」の3つの導線を配置する。

人気作品カードは、表紙、タイトル、ジャンル、キャッチコピー、閲覧数を主役にする。ルミはカードの中には入れず、作品がない時だけ本を抱えた姿で登場する。人気作品の見出しには、星を1つだけ置く。

ホームの推奨メッセージは「今日はどんな夢のページをひらく？」とする。検索欄のプレースホルダーは「タイトルやタグから探してみよう」に変更し、検索結果への導線を明確にする。

### 4.2 作品検索 `search.html`

検索ページのルミは、発見ポーズで大きな虫眼鏡を持つ。検索フォームの上部に小さく表示し、「どんな物語を探す？」という一言を添える。検索中はルミを動かさず、入力や結果更新を邪魔しない。

検索結果カードは、左に表紙、右にタイトルと作者を置く。タグは薄いピンクのチップ、ジャンルはミルキーブルーのチップとして視覚的に区別する。閲覧数とお気に入り数はカードの下部に小さくまとめる。

検索結果がない場合は、困り顔のルミが星の地図を見ているイラストを表示する。メッセージは「まだこの夢のページは見つからないみたい。別の言葉でも探してみよう」とする。検索条件を消すボタンも同じカード内に置く。

### 4.3 作品ページ `reader.html`

作品ページでは、作品そのものを最優先し、ルミは表紙の横に小さく本を抱えて配置する。作品の表紙を大きく見せ、タイトル、作者、ジャンル、キャッチコピー、あらすじ、タグ、セルフレイティング、オリジナル表示、閲覧数を情報のまとまりとして整理する。

お気に入りボタンは表紙情報の近くに置き、ハートとお気に入り数を同じ行に表示する。共有ボタンはその隣に置き、クリック後に作品タイトルと固有URLを表示する。URLのコピー成功時は、ルミの応援ポーズを小さく表示し、「リンクをコピーしたよ♡」と伝える。

チャプター選択は、横スクロール可能なタブにする。公開済みのチャプターだけを表示し、下書きは数も含めて読者に見せない。本文の下には、いいねボタン、いいね数、コメント欄の順で置く。コメントの作者削除は、作者本人にだけ薄い管理ボタンとして表示する。

本文中の作品リンクは、通常の本文と同じ文字色にせず、淡いピンクの下線を付ける。リンク先は必ず固有の作品ページに変換し、リンクカードには小さな「本をひらく」アイコンを付ける。リンクの装飾は文章の流れを妨げない大きさにする。

### 4.4 小説を書く `write.html`

執筆ページのルミは、ドリームペンで空中に文章を書いている。ページ見出しの近くに表示し、「今日はどんな物語を書く？」という案内を置く。入力のたびにルミを動かすのではなく、保存成功、公開成功、入力エラーの時だけ表情を変える。

小説情報、表紙、ジャンル、キャッチコピー、あらすじ、ハッシュタグ、セルフレイティング、公開設定は、カードを分けて段階的に表示する。公開設定カードでは、公開、限定、非公開の違いを短い説明で示す。非公開を選んだ場合は、困り顔ではなく安心した表情にし、「この物語は、あなたの下書きとして保存されるよ」と表示する。

チャプター編集では、左側にチャプター一覧、右側に編集領域を置く。スマートフォンでは一覧を上、編集領域を下に積む。公開ボタンは保存ボタンより強く見せすぎず、作品が完成したときに押す行動として扱う。

### 4.5 本棚 `bookshelf.html`

本棚のルミは、本の上に座って足を揺らしている。最近読んだ作品がある場合は、棚の端に小さく置く。お気に入りがある場合は、ハートの付いた本を抱える姿に切り替える。

最近読んだ作品とお気に入り作品は、タブの色で区別する。最近読んだはミルキーブルー、お気に入りはピンクを基調にする。作品カードには最終閲覧日時を表示するが、主要情報より小さくする。最後に読んだチャプターがある場合は、「第○話から読む」と表示する。

本棚が空の場合は、ルミが空の棚に小さな星を置いているイラストを表示する。最近読んだ場合は「ここから物語を探してみよう」、お気に入りの場合は「気になる作品をハートでしまおう♡」と案内する。

### 4.6 マイページ `mypage.html`

マイページは「あなたの夢のお部屋」として設計する。プロフィール画像と背景はユーザーの個性を優先し、ルミはプロフィールカードの端に小さく星を持って置く。ユーザーのプロフィール画像が大きい場合、ルミは重ならない位置に配置する。

統計は、作品数、お気に入り、フォロー、フォロワーを基本の4項目とする。自分の作品に関する閲覧数、お気に入り数、チャプターいいね数は、作者向けの追加統計カードにまとめる。読者向けのお気に入り数と、作者向けの作品評価を同じ数字として誤認させないよう、ラベルを明確にする。

作品一覧では、公開作品と保存作品を分ける。公開作品には「作品を見る」、下書きには「管理ページを開く」と表示する。ルミは公開作品がない時だけ登場し、「あなたの物語を、ここから世界へ届けよう」と案内する。

### 4.7 キャラクター管理 `characters.html`

キャラクター管理では、ルミがキャラクター設定用の小さなノートを持つ。キャラクターカードは、名前、立場、アイコン、メモを整理し、編集操作をカード右上にまとめる。

キャラクターが未登録の場合は、ルミが空のノートを開いている姿を表示する。案内文は「登場人物の夢を、ここに書き留めよう」とし、「キャラクターを追加」ボタンを1つだけ置く。キャラクターの入力画面に装飾を増やしすぎず、執筆時の集中を優先する。

### 4.8 アカウント選択・ログイン

アカウント関連のUIは、ルミが星の鍵を持っている構図にする。ログイン前は「夢の記録を保存するにはログインしてね」と説明する。アカウント切り替え時は、現在のアカウント名を明確に表示し、ゲスト状態と混同させない。

認証情報そのものをルミの吹き出しに表示してはいけない。ルミは状態説明だけを担当し、アカウント情報は通常のUIで扱う。

## 5. 状態別のルミ演出

### 初回訪問

ルミが本を開き、「ここから夢のページが始まるよ」と表示する。表示は初回だけにし、毎回出す場合は閉じるボタンを必ず付ける。

### 保存成功

ドリームペンの先から星が2〜3個出る短いアニメーションを表示する。メッセージは「保存したよ。続きはいつでもここから書けるよ」とする。

### 公開成功

ルミが本を掲げる祝福ポーズを表示する。「この夢、ちゃんと本になったね」と伝える。公開後は作品ページへのボタンを表示する。

### お気に入り・いいね成功

ハートが1回だけ弾み、周囲に小さな星を1個表示する。ルミの全身画像を出す必要はなく、画面の端に小さな顔アイコンまたはハートだけを出す。

### エラー

赤い警告表現や泣き顔は使わない。ルミは少し困った表情で、次に必要な操作を説明する。たとえば「作品が見つからないみたい。公開設定を確認してみよう」とする。

## 6. スマートフォン対応

スマートフォンでは、ルミのイラストをデスクトップの60〜70%程度に縮小する。作品ページでは表紙を情報の上に置き、ボタンを横幅いっぱいに近いサイズにする。お気に入りと共有は同じ行に置くが、画面幅が狭い場合は2段に分ける。

ナビゲーションは横スクロール可能なタブにし、重要な操作を画面外へ隠さない。チャプタータブは、現在位置が見えるように選択中のタブへ自動スクロールする。コメント本文は画面幅の90%を超えないようにし、編集・削除ボタンはコメント本文の下に置く。

ルミの装飾アニメーションは、`prefers-reduced-motion: reduce` が指定されている場合に停止する。アニメーションが停止しても、色、文言、状態表示だけで操作結果が分かるようにする。

## 7. 実装の優先順位

第一段階では、共通カラー、ボタン、カード、見出し、タグ、空状態を整え、ルミの静止画をホーム、作品ページ、執筆ページ、本棚、マイページ、検索ページへ配置する。第二段階では、表情差分と成功・空状態の案内カードを追加する。第三段階で、星やハートの短いアニメーションを追加し、スマートフォン表示を調整する。

最初から全ページに大きな立ち絵を配置するのではなく、**空状態と重要な完了状態から導入する**。これにより、作品を読む画面や執筆画面で本文と入力操作が埋もれない。ルミがいない画面もブランドから外れるわけではなく、共通カラー、星、ハート、雲、リボンのモチーフによってYUME WORLDとして認識できるようにする。

## 8. 完成時の確認項目

- ルミの髪、王冠、リボン、羽、ドリームペンが、縮小表示でも識別できる。
- 各ページでルミの役割が重複せず、ホームは案内、検索は発見、執筆は創作、本棚は読書、マイページは記録として使い分けられている。
- 作品ページでは表紙と本文がルミより目立つ。
- お気に入り、いいね、共有、公開、保存の状態変化が、アニメーションなしでも分かる。
- スマートフォンでボタン、チャプター、コメント、共有URLが操作しやすい。
- 非公開作品や下書きチャプターの情報が、ルミの案内文から漏れない。
- `prefers-reduced-motion` に対応し、動きが苦手な利用者にも同じ情報を提供できる。

## References

本設計書は、ユーザー提供のYUME WORLD公式キャラクター案と、既存YUME WORLDサイトのページ構成を基に作成した。

[1]: https://github.com/rina647/yume-story "YUME WORLD GitHub repository"
